﻿# 数据库 Agent Web 应用 — 需求说明书 V2.0

> **版本说明**：针对 V1.0 的 22 项需求模糊点进行了全面修订，修复了章节编号、功能边界、数据模型、API 设计、非功能需求、UI 交互、验收标准等方面的问题。

---

## 1. 产品定位与目标用户

### 1.1 产品定位
一款**开源的、智能的、跨数据库的 Agent Web 应用**，让用户通过自然语言或传统 SQL 编辑器与数据库交互，智能完成数据查询、表结构管理、性能分析和报表导出等任务。

### 1.2 目标用户
| 用户角色 | 核心需求 | 使用场景 |
|----------|----------|----------|
| 后端开发者 | 快速查询数据、编写和优化 SQL、管理表结构 | 日常开发、数据排障 |
| DBA / 数据库管理员 | 管理多个数据库实例、监控表结构变更、导出文档 | 数据库运维、变更审计 |
| 数据分析师 | 通过自然语言查询数据、生成可视化报表 | 数据探索、报表制作 |
| 产品经理（轻量） | 不熟悉 SQL 但需要查看数据辅助决策 | 数据查询、简单分析 |

### 1.3 核心价值
- **AI 驱动**：自然语言 → SQL、SQL 解释、自动优化建议、异常诊断
- **多数据库统一**：一套工具管理 Oracle、OceanBase、MySQL、GoldenDB、PostgreSQL、SQLite、MongoDB 等
- **高效工作台**：集成 SQL 编辑器、表结构管理、数据导出、报表面板
- **轻量可扩展**：Python 后端，插件化数据库适配，易于二次开发

---

## 2. 功能需求

### 2.1 功能全景图

```
数据库 Agent Web 应用
├── 2.2 连接管理
│   ├── 2.2.1 数据源 CRUD
│   ├── 2.2.2 连接测试
│   ├── 2.2.3 SSH 隧道支持
│   ├── 2.2.4 多环境管理（开发/测试/生产）
│   └── 2.2.5 连接信息加密存储
├── 2.3 SQL 工作台
│   ├── 2.3.1 SQL 编辑器（语法高亮/自动补全）
│   ├── 2.3.2 SQL 执行与结果展示
│   ├── 2.3.3 多 Tab 管理
│   ├── 2.3.4 SQL 格式化
│   ├── 2.3.5 执行历史
│   └── 2.3.6 结果集编辑与导出
├── 2.4 数据库对象管理
│   ├── 2.4.1 数据库/Schema 树
│   ├── 2.4.2 表结构可视化（列/索引/约束）
│   ├── 2.4.3 表结构编辑与 DDL 生成
│   ├── 2.4.4 视图/函数/存储过程/触发器管理
│   └── 2.4.5 序列管理
├── 2.5 AI Agent
│   ├── 2.5.1 自然语言 → SQL 生成
│   ├── 2.5.2 SQL 解释与优化建议
│   ├── 2.5.3 数据库异常诊断
│   ├── 2.5.4 Agent 对话上下文管理
│   ├── 2.5.5 Schema 感知（自动注入表结构）
│   └── 2.5.6 多模型支持（OpenAI/Claude/本地模型）
├── 2.6 文档导出
│   ├── 2.6.1 Word 格式导出
│   ├── 2.6.2 Excel 格式导出
│   ├── 2.6.3 CSV/JSON 格式导出
│   └── 2.6.4 导出范围选择（全库/选表）
├── 2.7 可视化报表
│   ├── 2.7.1 图表展示（柱状图/折线图/饼图）
│   ├── 2.7.2 自定义 SQL → 图表
│   ├── 2.7.3 仪表盘面板
│   └── 2.7.4 报表分享
├── 2.8 系统管理
│   ├── 2.8.1 用户登录/登出
│   ├── 2.8.2 AI 配置管理
│   ├── 2.8.3 JDBC 驱动管理
│   └── 2.8.4 系统设置
└── 2.9 扩展能力
    ├── 2.9.1 数据库插件接口
    └── 2.9.2 自定义 Agent 工具
```

> **修订说明**：全景图编号从 3.x.x 修正为 2.x.x，与所属章节编号一致。

---

### 2.2 连接管理

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 数据源列表 | P0 | 分页展示已保存的数据源连接，支持搜索和按环境过滤 |
| 新建连接 | P0 | 支持 Oracle、OceanBase、GoldenDB（P0）；MySQL、PostgreSQL、SQLite、MongoDB（P1）；填写主机、端口、用户名、密码、默认数据库 |
| 连接测试 | P0 | 点击测试连接按钮，实时返回连接成功/失败信息 |
| 连接详情 | P1 | 查看/编辑已有连接的完整配置 |
| 删除连接 | P0 | 删除连接配置 |
| 克隆连接 | P1 | 基于已有连接快速创建新连接；API: `POST /api/connections/:id/clone` |
| SSH 隧道 | P1 | 支持通过 SSH 跳板机连接内网数据库；支持密码和密钥认证 |
| 多环境 | P1 | 连接可标记为开发/测试/生产等环境，环境颜色区分 |
| 密码加密 | P0 | 所有密码使用 Fernet / AES 加密存储 |

> **修订说明**：明确各数据库类型在各阶段的优先级；补充克隆连接 API。

---

### 2.3 SQL 工作台

| 功能 | 优先级 | 说明 |
|------|--------|------|
| SQL 编辑器 | P0 | 基于 CodeMirror 6，支持语法高亮、括号匹配、SQL 关键词自动补全、行号 |
| SQL 执行 | P0 | 执行当前编辑器中的 SQL（支持多条 SQL），返回结果集 |
| 结果展示 | P0 | 表格形式展示查询结果，支持虚拟滚动、列排序、列宽拖拽、分页加载 |
| 多 Tab | **P0** | 支持同时打开多个 SQL 编辑器 Tab，Tab 之间独立会话（UI 原型展示为核心功能，提升为 P0） |
| SQL 格式化 | P1 | 一键格式化 SQL 语句 |
| 执行历史 | P1 | 记录 SQL 执行历史，支持按时间、数据源筛选，可重新执行 |
| 行数统计 | P1 | 显示查询影响行数/总行数 |
| 结果集编辑 | P2 | 直接在结果表格中编辑单元格数据，自动生成 UPDATE SQL |
| 结果导出 | P1 | 导出查询结果为 CSV / Excel / JSON 格式 |

> **修订说明**：多 Tab 从 P1 提升为 P0、与 UI 原型保持一致。

---

### 2.4 数据库对象管理

#### 2.4.1 数据库对象树
- 连接后展示数据库 → Schema → 对象的层级树
- 对象类型包括：表、视图、函数、存储过程、触发器、序列
- 树节点数据每次从数据库动态查询，不持久化（避免与源库结构不同步）
- 支持右键菜单（查看表结构、导出 DDL、删除等）

#### 2.4.2 表结构管理
- 展示表的列信息（名称、类型、是否可为空、默认值、主键、自增、注释）
- 展示索引信息（名称、类型、列、是否唯一）
- 展示约束信息（主键、外键、唯一约束、检查约束）
- 支持收藏表（Pin），收藏状态持久化到本地 SQLite（仅存储表名和连接 ID）
- 收藏表实体见第 5 章

#### 2.4.3 表结构编辑与 DDL 生成
- 可视化编辑表结构（增删列、修改列类型/默认值、增删索引）
- 自动生成 ALTER TABLE DDL
- 用户确认 DDL 后执行
- API：`POST /api/metadata/{datasource_id}/tables/{table}/alter`（请求体包含变更描述，返回生成的 DDL + 执行结果）

#### 2.4.4 视图/函数/存储过程/触发器管理
- 展现对象的 DDL 定义
- 支持查看和复制 DDL
- 元数据 API 补充：
  - `GET /api/metadata/{datasource_id}/triggers?schema=xxx`
  - `GET /api/metadata/{datasource_id}/triggers/{name}/ddl`
  - `GET /api/metadata/{datasource_id}/sequences?schema=xxx`
  - `GET /api/metadata/{datasource_id}/functions?schema=xxx`
  - `GET /api/metadata/{datasource_id}/procedures?schema=xxx`

#### 2.4.5 序列管理
- 查看序列当前值、增量、最小值、最大值、缓存大小
- 复制序列 DDL

> **修订说明**：明确对象树数据动态查询不持久化；补充收藏表持久化策略；补充触发器/序列/函数/存储过程的完整 API 端点。

---

### 2.5 AI Agent（核心差异化功能）

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 自然语言→SQL | P0 | 用户输入自然语言描述，Agent 自动生成对应 SQL 并可在工作台执行 |
| 表结构感知 | P0 | Agent 自动获取当前数据库的 Schema 信息作为上下文，提升 SQL 准确率 |
| 多轮对话 | P0 | 支持上下文记忆，可在对话中追问、调整需求 |
| SQL 解释 | P1 | 选中/输入 SQL，Agent 用自然语言解释其含义 |
| SQL 优化建议 | P1 | Agent 分析 SQL 执行计划，给出索引优化、查询重写建议 |
| 异常诊断 | P2 | 输入数据库报错信息，Agent 辅助分析原因并给出解决方案 |
| 数据查询 Agent | P1 | "查询本月新增用户数并按日分组"→ Agent 自动推理 → 生成 SQL → 执行并返回结果 |
| 多模型支持 | P1 | 支持 OpenAI GPT-4o、Claude、Ollama 等（详见第 4.5 节模型切换策略） |
| 自定义 Prompt | P2 | 高级用户可自定义 Agent 的 system prompt |
| 流式输出 | P0 | AI 响应以 SSE 流式输出，用户实时看到生成过程 |

**AI Agent 架构示意**：
```
用户输入 → Agent 推理引擎（ReAct 模式）
    ├── Schema 感知工具（获取当前库表结构，只读）
    ├── SQL 只读执行工具（仅允许 SELECT/SHOW/DESC/EXPLAIN，执行前用户确认）
    ├── SQL 分析工具（分析执行计划）
    └── 文档工具（查询帮助文档）
         ↓
    Agent 综合推理 → 生成回复/SQL → SSE 流式返回
```


#### 2.5.1 SQL 执行授权机制

##### 2.5.1.1 安全策略总览

Agent 提供的 SQL 执行遵循以下安全策略：

| 操作类型 | 授权方式 | 说明 |
|----------|----------|------|
| SELECT / SHOW / DESC / EXPLAIN | **自动执行** | Agent 可直接执行只读查询 |
| INSERT / UPDATE / DELETE | **手动确认** | Agent 生成 DML 后，需用户在 UI 弹窗中点击"确认执行" |
| CREATE / ALTER / DROP / TRUNCATE | **手动确认 + 高亮警告** | DDL 操作弹窗红色警告，展示完整 DDL 内容，用户手动确认 |
| 多条混合 SQL | **逐条确认** | 按 SQL 类型分别走上述规则 |

##### 2.5.1.2 前后端交互协议

**执行流程**：

```
┌──────────┐          ┌──────────┐          ┌──────────┐
│  前端     │          │  后端 API │          │  目标数据库│
│ (Vue UI)  │          │ (FastAPI) │          │          │
└────┬─────┘          └────┬─────┘          └────┬─────┘
     │                      │                      │
     │ 1. POST /api/sql/execute                   │
     │    { datasource_id, sql, page, page_size } │
     │─────────────────────>│                     │
     │                      │                     │
     │ 2. 后端检测 SQL 类型                       │
     │    是否 SELECT? → 直接执行                  │
     │    是否 DML/DDL? → 返回确认请求             │
     │                      │                     │
     │ 3a. [只读查询 - 直接返回结果]               │
     │    { code: 0, data: {                      │
     │      need_confirm: false,                  │
     │      operation_type: "SELECT",             │
     │      columns: [...], rows: [...],          │
     │      total_rows: N, duration_ms: N         │
     │    }}                                      │
     │<─────────────────────│                     │
     │                      │                     │
     │ 3b. [写操作 - 返回确认请求]                 │
     │    { code: 0, data: {                      │
     │      need_confirm: true,                   │
     │      operation_type: "DML" / "DDL",        │
     │      sql_text: "DELETE FROM users...",      │
     │      preview: "将影响 5 行数据",            │
     │      risk_level: "warning" / "danger",     │
     │      execution_id: "uuid-xxx"              │
     │    }}                                      │
     │<─────────────────────│                     │
     │                      │                     │
     │ 4. [用户确认后]                              │
     │ POST /api/sql/execute/confirm              │
     │    { execution_id: "uuid-xxx",             │
     │      confirmed: true }                     │
     │─────────────────────>│                     │
     │                      │────────────────────>│
     │                      │ 执行 SQL            │
     │                      │<────────────────────│
     │ 5. { code: 0, data: {                      │
     │      need_confirm: false,                  │
     │      operation_type: "DML",               │
     │      affected_rows: 5,                    │
     │      duration_ms: 120                     │
     │    }}                                      │
     │<─────────────────────│                     │
     │                      │                     │
     │ 6. 所有操作记录审计日志                      │
     │    audit_log (action_type, sql_text,        │
     │     operation_type, status: approved/rejected)│
```

**请求-响应 Schema 定义**：

**POST /api/sql/execute** — SQL 执行请求：
```json
{
  "datasource_id": 1,
  "sql": "DELETE FROM users WHERE id = 1",
  "page": 1,
  "page_size": 100,
  "max_rows": 1000
}
```

**响应 — 只读查询（自动执行）**：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "need_confirm": false,
    "operation_type": "SELECT",
    "columns": ["id", "name", "email"],
    "rows": [
      [1, "Alice", "alice@example.com"],
      [2, "Bob", "bob@example.com"]
    ],
    "total_rows": 100,
    "page": 1,
    "page_size": 100,
    "duration_ms": 45
  }
}
```

**响应 — 写操作（需确认）**：
```json
{
  "code": 0,
  "message": "需要确认执行",
  "data": {
    "need_confirm": true,
    "operation_type": "DML",
    "sql_text": "DELETE FROM users WHERE id = 1",
    "preview": "将影响 1 行数据",
    "risk_level": "warning",
    "execution_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
  }
}
```

其中 `risk_level` 取值：
- `"info"`：SELECT 等只读操作（自动执行，无需确认）
- `"warning"`：DML 操作（弹窗确认，蓝色提示）
- `"danger"`：DDL 操作（红色警告边框 + 二次确认勾选框）

**POST /api/sql/execute/confirm** — 用户确认执行：
```json
{
  "execution_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
  "confirmed": true
}
```

**响应**：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "need_confirm": false,
    "operation_type": "DML",
    "affected_rows": 1,
    "duration_ms": 120
  }
}
```

**用户拒绝执行**（confirmed: false）时：
- 后端不执行 SQL，仅记录 audit_log（status: "rejected"）
- 返回 `{ code: 0, message: "已取消执行", data: { status: "cancelled" } }`

**execution_id 生命周期**：生成后 5 分钟超时，超时后返回 `{ code: 408, message: "确认超时，请重新执行" }`

> **修订说明**：补充完整的前后端交互协议，包括请求-响应 Schema、execution_id 机制、risk_level 分级、确认端点定义。

---

#### 2.5.2 Agent 执行结果回传协议

Agent 通过 SSE（Server-Sent Events）与前端通信。当 Agent 调用 `execute_sql_tool` 执行查询后，后端需要将结果通过 SSE 事件流式返回给前端。

##### 2.5.2.1 SSE 事件类型

| 事件类型 | 说明 | 数据格式 |
|----------|------|----------|
| `thought` | Agent 思考过程 | `{ "type": "thought", "content": "用户想查询本月新增用户..." }` |
| `sql` | Agent 生成的 SQL | `{ "type": "sql", "content": "SELECT ...", "operation_type": "SELECT", "need_confirm": false }` |
| `result` | 查询结果 | `{ "type": "result", "content": { columns, rows, total_rows, duration_ms }, "truncated": false }` |
| `authorization_required` | SQL 需要用户确认 | `{ "type": "authorization_required", "execution_id": "uuid", "sql_text": "...", "operation_type": "DML", "risk_level": "warning", "preview": "将影响 N 行" }` |
| `execution_result` | 用户确认后执行的结果 | `{ "type": "execution_result", "content": { affected_rows, duration_ms } }` |
| `error` | 错误信息 | `{ "type": "error", "content": "SQL 执行错误: ORA-00942 表不存在" }` |
| `done` | 本轮响应结束 | `{ "type": "done" }` |

**SSE 响应示例**（完整一次对话流）：
```
data: {"type":"thought","content":"用户想查询本月新增用户数并按日分组，我先查看表结构..."}

data: {"type":"thought","content":"找到了 users 表，有 created_at 字段，我生成 SQL"}

data: {"type":"sql","content":"SELECT DATE(created_at) AS day, COUNT(*) AS new_users\nFROM users\nWHERE created_at >= DATE_TRUNC('month', CURRENT_DATE)\nGROUP BY DATE(created_at)\nORDER BY day","operation_type":"SELECT","need_confirm":false}

data: {"type":"result","content":{"columns":["day","new_users"],"rows":[["2026-07-01",15],["2026-07-02",22],["2026-07-03",18]],"total_rows":17,"duration_ms":120},"truncated":false}

data: {"type":"done"}
```

##### 2.5.2.2 result 事件详细格式

```json
{
  "type": "result",
  "content": {
    "columns": [
      { "name": "day", "data_type": "DATE" },
      { "name": "new_users", "data_type": "INTEGER" }
    ],
    "rows": [
      ["2026-07-01", 15],
      ["2026-07-02", 22]
    ],
    "total_rows": 17,
    "display_rows": 2,
    "duration_ms": 120,
    "sql_text": "SELECT DATE(created_at)..."
  },
  "truncated": false,
  "message": "查询返回 17 行，显示前 2 行"
}
```

##### 2.5.2.3 大结果集处理

| 场景 | 处理方式 |
|------|----------|
| 结果行数 ≤ 100 | 完整返回，`truncated: false` |
| 结果行数 100 ~ 1000 | 返回前 100 行，`truncated: true`，`message: "查询返回 N 行，显示前 100 行"` |
| 结果行数 > 1000 | 返回前 100 行，`truncated: true`，`message: "结果集过大（N 行），显示前 100 行，请使用分页加载"` |
| 结果列数 > 50 | 仅返回前 50 列，`truncated: true`，并在 Agent 回复中说明省略的列 |
| 单元格数据 > 1KB | 截断到 1KB，末尾添加 `...(truncated)` |

Agent 在返回大结果集时，会在回复文本中自动提示用户使用 SQL 工作台的分页功能查看完整数据。

##### 2.5.2.4 前端渲染要求

- `thought` 事件：打字机效果逐字显示
- `sql` 事件：代码块高亮显示，附带「填入编辑器」「执行」按钮
- `result` 事件：渲染为 NDataTable 或简单表格（根据行数自动选择）
- `authorization_required` 事件：弹窗显示 SQL 内容和风险等级
- `execution_result` 事件：在对话中插入"✅ 已执行：影响 N 行"消息
- `error` 事件：红色错误提示
- `done` 事件：停止 loading 状态，启用输入框

> **修订说明**：补充 Agent 执行结果回传协议，包括 SSE 事件类型、result 格式、大结果集截断策略、前端渲染要求。

---

### 2.6 文档导出

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 导出数据库文档 | P1 | 将数据库中所有表的结构导出为文档 |
| 选择表导出 | P1 | 只导出选中的表 |
| Word 导出 | P1 | 使用 python-docx 生成格式化的 Word 文档 |
| Excel 导出 | P1 | 使用 openpyxl 生成 Excel，一表一 Sheet |
| CSV 导出 | P2 | 生成 CSV 格式文件 |
| JSON 导出 | P2 | 生成 JSON 格式文件 |
| Markdown 导出 | P2 | 生成 Markdown 格式的数据库文档 |
| HTML 导出 | P2 | 生成带样式的 HTML 文档 |
| 导出内容 | P1 | 包含表名、注释、列信息、索引信息、DDL |
| 断点续传 | P3 | 仅在 Excel/CSV 大数据量导出时支持，指导出任务中断后可恢复进度，非下载断点续传 |

**导出 API 设计**：
| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/export/result` | POST | 导出查询结果集 |
| `/api/export/database` | POST | 导出数据库文档 |
| `/api/export/database/status/{task_id}` | GET | 查询导出任务状态（用于大导出） |

格式区分方式：统一通过查询参数 `?format=excel|csv|json|word|markdown|html` 指定，不再依赖 Header 或路径后缀。

> **修订说明**：补充导出 API 端点和格式区分方式；澄清"断点续传"为指导任务中断可恢复，非下载断点续传。

---

### 2.7 可视化报表

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 创建图表 | P2 | 基于 SQL 查询结果生成柱状图、折线图、饼图 |
| 仪表盘 | P2 | 将多个图表组合为一个仪表盘面板 |
| 图表配置 | P2 | 选择图表类型、X/Y 轴、聚合方式 |
| 分享 | P3 | 将仪表盘分享为只读链接 |

**报表相关实体**（见第 5 章数据模型）：
- Chart：图表配置（名称、SQL、类型、X/Y 轴配置、数据源 ID、创建时间）
- Dashboard：仪表盘（名称、图表列表、布局配置、创建时间）

> **修订说明**：补充 Chart/Dashboard 实体定义。

---

### 2.8 系统管理

#### 2.8.1 用户认证
| 功能 | 优先级 | 说明 |
|------|--------|------|
| 用户登录 | P0 | 用户名 + 密码登录；支持 Session/Cookie 认证 |
| 用户登出 | P0 | 清除服务端会话 |
| 用户注册 | P2 | 新用户注册（初期使用默认账户，无需注册） |
| 桌面端免登录 | **已移除** | 项目定位为 Web 应用，无桌面端包装；如未来需要 Electron 包装，通过 Token 文件实现免登录 |

> **修订说明**：移除矛盾的"桌面端免登录"描述，明确项目定位为纯 Web 应用。

#### 2.8.2 AI 配置管理
| 功能 | 优先级 | 说明 |
|------|--------|------|
| 提供商管理 | P0 | 配置 OpenAI、Claude、Ollama 的 API Key、端点、模型名称 |
| 默认模型 | P0 | 设置默认使用的 AI 模型 |
| 多模型切换 | P1 | 每个对话可独立选择模型，对话创建时选定，中途不可切换 |

**模型切换策略**：
- 系统级别：在系统设置中配置各提供商（OpenAI/Claude/Ollama）的 API Key 和默认模型
- 对话级别：创建新 Agent 对话时，用户可从已启用的模型列表中选择一个作为该对话的模型
- 配置实体 Ollama：需额外配置 `api_base`（如 `http://localhost:11434`）+ `model_name`（如 `qwen2.5-coder:14b`）
- 切换限制：对话创建后中途不可切换模型（上下文不一致），如需切换则新建对话

#### 2.8.3 JDBC 驱动管理
| 功能 | 优先级 | 说明 |
|------|--------|------|
| 驱动列表 | P1 | 展示已上传的 JDBC 驱动文件 |
| 上传驱动 | P1 | 上传 JDBC 驱动 JAR 文件 |
| 删除驱动 | P2 | 删除已上传的驱动 |
| 驱动自动匹配 | P2 | 选择数据库类型时自动推荐匹配的驱动 |

相关实体：JdbcDriver（id, db_type, driver_class, version, file_path, upload_time）

> **修订说明**：补充 JDBC 驱动管理的完整说明和实体定义。

#### 2.8.4 系统设置
| 功能 | 优先级 | 说明 |
|------|--------|------|
| 语言切换 | P1 | 中/英文界面切换 |
| 主题切换 | P1 | 亮色/暗色主题 |
| 编辑器偏好 | P2 | 字体大小、Tab 宽度、自动补全开关 |

---

### 2.9 扩展能力

#### 2.9.1 数据库插件接口
详见第 4 章技术架构设计 — 数据库适配层。

#### 2.9.2 自定义 Agent 工具
- 预留工具注册接口，方便社区贡献自定义 Agent 工具
- 工具需实现 BaseAgentTool 接口（tool_name, tool_description, arun()）
- P2 阶段实现

---

## 3. 非功能需求

### 3.1 安全性

| 需求 | 说明 |
|------|------|
| **密码加密** | 数据源密码使用 Fernet 对称加密后存储 |
| **SQL 执行安全** | 见第 2.5.1 节 SQL 执行授权机制 |
| | 由于 SQL 工作台的本质是执行用户输入的 SQL，不适用参数化查询防护 |
| | 防护策略：① 只读查询自动放行；② 写操作弹窗确认；③ 危险操作红色警告 + 二次确认 |
| | ④ 执行日志记录到 audit_log 表，支持事后审计 |
| **API 安全** | 所有 API 需携带 Session Token；CORS 白名单限制 |
| **HTTPS** | 生产环境强制 HTTPS |

> **修订说明**：澄清 SQL 注入防护策略，承认参数化查询在此场景不适用，代之以授权+审计的组合方案。

### 3.2 性能

| 需求 | 说明 |
|------|------|
| SQL 执行超时 | 默认 30s，可在系统设置中调整 |
| 大结果集 | 自动分页加载，单次最多返回 1000 行 |
| 前端构建 | 首次加载 ≤ 3s（非 5MB 硬限制）；对于 CodeMirror、ECharts 等大库，允许 CDN 外链 + 按需加载 |

> **修订说明**：将"前端构建产物 < 5MB"优化为"首次加载 ≤ 3s"，允许 CDN 和按需加载。

### 3.3 可扩展性
- 新增数据库类型只需实现 BaseDBAdapter 接口（无需改核心代码）
- 插件化 Agent 工具注册
- 适配器接口定义见第 4.3 节

### 3.4 可用性
- 响应式布局，最小宽度 1024px
- SSE 流式输出中 AI Agent 断线自动重连（3 次）
- 所有操作有明确 loading/error/empty 状态

---

## 4. 技术架构设计

### 4.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    前端（Vue 3 Web 应用）                      │
│  Vue 3 + TypeScript + Pinia + Vue Router + Naive UI         │
│  ┌──────────┐ ┌──────────┐ ┌────────┐ ┌──────────────────┐  │
│  │ SQL工作台  │ │ 报表面板  │ │ 连接管理 │ │ AI Agent 面板    │  │
│  │ (Workspace)│ │(Dashboard)│ │(Connection)│ │ (Chat)          │  │
│  └──────────┘ └──────────┘ └────────┘ └──────────────────┘  │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTP / SSE / WebSocket
┌──────────────────────────▼──────────────────────────────────┐
│                  后端 API 服务 (Python FastAPI)                │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ API 路由层                                              │  │
│  │ ┌────────┐┌────────┐┌────────┐┌─────────┐┌────────┐  │  │
│  │ │连接管理 ││ SQL执行 ││元数据   ││ 文档导出 ││ AI     │  │  │
│  │ │ Route   ││ Route  ││ Route  ││ Route   ││ Route  │  │  │
│  │ └────────┘└────────┘└────────┘└─────────┘└────────┘  │  │
│  └────────────────────────────────────────────────────────┘  │
│                           │                                   │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 业务服务层 (Service)                                    │  │
│  │ ┌──────────────┐ ┌──────────────┐ ┌────────────────┐  │  │
│  │ │ Connection   │ │ SQLExecutor  │ │ AI Agent       │  │  │
│  │ │ Service      │ │ Service      │ │ Service        │  │  │
│  │ └──────────────┘ └──────────────┘ └────────────────┘  │  │
│  └────────────────────────────────────────────────────────┘  │
│                           │                                   │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 数据库适配层 (DB Adapter Plugin)                         │  │
│  │ ┌──────────┐ ┌────────────┐ ┌──────────┐ ┌────────┐   │  │
│  │ │ Oracle   │ │ OceanBase  │ │ GoldenDB │ │ MySQL  │   │  │
│  │ │Adapter   │ │ Adapter    │ │ Adapter  │ │Adapter │   │  │
│  │ └──────────┘ └────────────┘ └──────────┘ └────────┘   │  │
│  │ ┌──────────┐ ┌──────────┐ ┌──────────┐               │  │
│  │ │PostgreSQL│ │ SQLite   │ │ MongoDB  │  ...           │  │
│  │ │Adapter   │ │ Adapter  │ │ Adapter  │               │  │
│  │ └──────────┘ └──────────┘ └──────────┘               │  │
│  └────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 前端架构

| 技术 | 选型 | 理由 |
|------|------|------|
| 框架 | Vue 3 + Composition API | 响应式、TypeScript 友好、社区活跃 |
| 构建工具 | Vite | 极速 HMR，开发体验好 |
| UI 组件库 | Naive UI | 组件丰富、TypeScript 支持好 |
| 状态管理 | Pinia | 轻量、TypeScript 原生支持 |
| 路由 | Vue Router 4 | Vue 3 官方路由 |
| 富文本/SQL 编辑器 | CodeMirror 6 | 轻量（比 Monaco Editor 小 80%）、Vue 3 生态好 |
| 图表 | ECharts | 图表类型丰富、中文文档完善 |
| 表格 | Naive UI NDataTable | 原生支持虚拟滚动、排序、列宽拖拽 |
| CDN 策略 | ECharts 通过 CDN 外链加载，减少主包体积 | |

### 4.3 后端架构

| 技术 | 选型 | 理由 |
|------|------|------|
| 框架 | FastAPI | 异步原生、自动 OpenAPI 文档、高性能 |
| ORM | SQLAlchemy 2.0 | 异步支持、声明式映射、Alembic 迁移 |
| 数据库 | SQLite（本地存储）+ Alembic（迁移管理） | 零配置、无需额外数据库服务器 |
| AI 框架 | LangChain | Agent、Tool、Memory 开箱即用 |
| 加密 | cryptography (Fernet) | 简单安全的对称加密 |
| 数据库驱动 | oracledb / asyncmy / asyncpg / aiosqlite | 各数据库异步驱动 |

### 4.4 数据库适配层（DB Adapter）

```
BaseDBAdapter（抽象基类）
├── OracleAdapter（P0）
├── OceanBaseAdapter（P0，可复用 BaseMySQLAdapter）
├── GoldenDBAdapter（P0，可复用 BaseMySQLAdapter）
├── MySQLAdapter（P1）
├── PostgreSQLAdapter（P1）
├── SQLiteAdapter（P1）
└── MongoDBAdapter（P2）
```

**P0 阶段仅实现**：OracleAdapter、OceanBaseAdapter、GoldenDBAdapter
**P1 阶段增加**：MySQLAdapter、PostgreSQLAdapter、SQLiteAdapter
**P2 阶段增加**：MongoDBAdapter

> **修订说明**：明确各阶段需要实现的数据库适配器。

### 4.5 AI 模型切换策略

| 维度 | 策略 |
|------|------|
| 配置入口 | 系统设置 → AI 配置 → 添加提供商 |
| 支持的提供商 | OpenAI（GPT-4o / GPT-4o-mini）、Anthropic（Claude 3.5 Sonnet）、Ollama（本地模型） |
| Ollama 配置 | 需填写 `api_base`（默认 `http://localhost:11434`）+ `model_name` |
| 默认模型 | 系统设置中选择一个已配置的模型作为默认 |
| 对话级选择 | 创建新 Agent 对话时，下拉选择该对话使用的模型 |
| 切换限制 | 对话创建后不可切换模型（上下文不一致），切换需新建对话 |
| 接口统一 | 所有提供商通过 LangChain 统一接口调用 |

> **修订说明**：补充完整的模型切换策略。

---

## 5. 数据模型设计

### 5.1 核心实体

#### 5.1.1 DataSource（数据源）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer (PK) | 自增主键 |
| name | String(100) | 连接名称，唯一 |
| db_type | String(30) | 数据库类型：oracle / oceanbase / goldendb / mysql / postgresql / sqlite / mongodb |
| host | String(255) | 主机地址 |
| port | Integer | 端口号 |
| username | String(100) | 用户名 |
| encrypted_password | Text | Fernet 加密后的密码 |
| database_name | String(100) | 默认数据库/服务名 |
| ssh_enabled | Boolean | 是否启用 SSH 隧道 |
| ssh_host | String(255) | SSH 跳板机主机 |
| ssh_port | Integer | SSH 端口，默认 22 |
| ssh_user | String(100) | SSH 用户名 |
| ssh_auth_type | String(20) | 认证方式：password / key |
| ssh_private_key | Text | SSH 私钥内容（加密存储） |
| environment | String(20) | 环境标签：dev / test / prod |
| status | String(20) | 状态：active / error / unknown |
| description | Text | 描述 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

#### 5.1.2 User（用户）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer (PK) | 自增主键 |
| username | String(50) | 用户名，唯一 |
| password_hash | String(255) | bcrypt 哈希后的密码 |
| display_name | String(100) | 显示名称 |
| is_active | Boolean | 是否启用 |
| role | String(20) | 角色：admin / user |
| created_at | DateTime | 创建时间 |
| last_login | DateTime | 最后登录时间 |

> **修订说明**：V1.0 缺失 User 实体，V2.0 补充。

#### 5.1.3 AIConfig（AI 模型配置）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer (PK) | 自增主键 |
| provider | String(20) | 提供商：openai / claude / ollama |
| api_key | String(255) | API Key（加密存储） |
| api_base | String(255) | API 端点 |
| model_name | String(100) | 模型名称 |
| max_tokens | Integer | 最大 Token 数，默认 4096 |
| temperature | Float | 温度参数，默认 0.7 |
| is_active | Boolean | 是否启用 |
| is_default | Boolean | 是否为默认模型 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

> **修订说明**：V1.0 缺失 AIConfig 实体，V2.0 补充。

#### 5.1.4 JdbcDriver（JDBC 驱动）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer (PK) | 自增主键 |
| db_type | String(30) | 对应数据库类型 |
| driver_class | String(200) | 驱动类名 |
| version | String(20) | 驱动版本 |
| file_path | String(500) | 驱动文件存储路径 |
| file_size | Integer | 文件大小（字节） |
| upload_time | DateTime | 上传时间 |

> **修订说明**：V1.0 缺失 JdbcDriver 实体，V2.0 补充。

#### 5.1.5 QueryHistory（SQL 执行历史）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer (PK) | 自增主键 |
| datasource_id | Integer (FK) | 数据源 ID |
| sql_text | Text | SQL 内容 |
| status | String(20) | 成功 / 失败 |
| row_count | Integer | 影响/返回行数 |
| duration_ms | Integer | 执行耗时（毫秒） |
| error_message | Text | 错误信息（失败时） |
| created_at | DateTime | 执行时间 |

#### 5.1.6 AgentSession（Agent 对话会话）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer (PK) | 自增主键 |
| title | String(200) | 会话标题（自动生成或用户命名） |
| datasource_id | Integer (FK) | 关联的数据源 |
| model_config_id | Integer (FK) | 该对话使用的 AI 配置 |
| message_count | Integer | 消息总数 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 最后活动时间 |

#### 5.1.7 AgentMessage（Agent 对话消息）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer (PK) | 自增主键 |
| session_id | Integer (FK) | 所属会话 |
| role | String(20) | 角色：user / assistant / system |
| content | Text | 消息内容 |
| message_type | String(20) | 类型：text / sql / table / error |
| created_at | DateTime | 发送时间 |

#### 5.1.8 AuditLog（审计日志）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer (PK) | 自增主键 |
| user_id | Integer (FK) | 操作人 |
| action_type | String(30) | 操作类型：execute_sql / export / agent_action |
| sql_text | Text | 执行的 SQL（如适用） |
| operation_type | String(20) | SQL 类型：SELECT / DML / DDL |
| datasource_id | Integer (FK) | 操作的数据源 |
| status | String(20) | 结果：approved / rejected / success / failed |
| client_ip | String(45) | 客户端 IP |
| created_at | DateTime | 操作时间 |

#### 5.1.9 FavoritedTable（收藏表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer (PK) | 自增主键 |
| datasource_id | Integer (FK) | 数据源 |
| schema_name | String(100) | Schema 名称 |
| table_name | String(100) | 表名 |
| created_at | DateTime | 收藏时间 |

#### 5.1.10 Chart（图表配置）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer (PK) | 自增主键 |
| name | String(100) | 图表名称 |
| datasource_id | Integer (FK) | 数据源 |
| sql_text | Text | 查询 SQL |
| chart_type | String(20) | 类型：bar / line / pie |
| x_column | String(100) | X 轴列名 |
| y_column | String(100) | Y 轴列名 |
| aggregation | String(20) | 聚合方式：sum / avg / count / none |
| options | Text (JSON) | 额外配置（颜色、标题等） |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

#### 5.1.11 Dashboard（仪表盘）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer (PK) | 自增主键 |
| name | String(100) | 仪表盘名称 |
| chart_ids | Text (JSON) | 包含的图表 ID 列表 |
| layout | Text (JSON) | 图表布局配置 |
| is_public | Boolean | 是否公开分享 |
| share_token | String(64) | 分享令牌 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

> **修订说明**：V1.0 缺失 Chart/Dashboard 实体，V2.0 补充。

### 5.2 ER 关系图

```
DataSource 1 ── * QueryHistory
DataSource 1 ── * AgentSession
DataSource 1 ── * FavoritedTable
DataSource 1 ── * Chart
AIConfig   1 ── * AgentSession
AgentSession 1 ── * AgentMessage
User       1 ── * AuditLog
User       1 ── * DataSource (created_by)
Dashboard  * ── * Chart (through chart_ids)
```

---

## 6. API 设计

### 6.1 统一规范

| 规范 | 说明 |
|------|------|
| 基础路径 | `/api` |
| 响应格式 | `{ code: 0, message: "success", data: {} }` |
| 错误码 | 0=成功, 400=参数错误, 401=未登录, 403=无权限, 404=不存在, 500=服务端错误 |
| 认证 | Session/Cookie，请求头携带 `Cookie: session=xxx` |
| 分页 | 请求：`?page=1&page_size=20`；响应：`{ ..., data: { list: [...], total: 100, page: 1, page_size: 20 } }` |

### 6.2 API 端点清单

#### 6.2.1 连接管理
| 方法 | 端点 | 说明 | 优先级 |
|------|------|------|--------|
| POST | `/api/connections` | 新建数据源 | P0 |
| GET | `/api/connections` | 分页列表（?search=&environment=） | P0 |
| GET | `/api/connections/:id` | 详情 | P1 |
| PUT | `/api/connections/:id` | 更新 | P1 |
| DELETE | `/api/connections/:id` | 删除 | P0 |
| POST | `/api/connections/:id/clone` | 克隆连接 | P1 |
| POST | `/api/connections/test` | 测试连接 | P0 |
| POST | `/api/connections/:id/connect` | 建立连接并获取 Schema | P0 |

#### 6.2.2 SQL 工作台
| 方法 | 端点 | 说明 | 优先级 |
|------|------|------|--------|
| POST | `/api/sql/execute` | 执行 SQL | P0 |
| POST | `/api/sql/execute/confirm` | 用户确认后执行写操作/DDL（请求体：{ execution_id, confirmed }） | P0 |
| POST | `/api/sql/format` | SQL 格式化 | P1 |
| GET | `/api/sql/history` | 执行历史（?datasource_id=&page=） | P1 |

#### 6.2.3 元数据
| 方法 | 端点 | 说明 | 优先级 |
|------|------|------|--------|
| GET | `/api/metadata/{ds_id}/schemas` | Schema 列表 | P0 |
| GET | `/api/metadata/{ds_id}/tables` | 表列表（?schema=） | P0 |
| GET | `/api/metadata/{ds_id}/tables/{table}/columns` | 列信息 | P0 |
| GET | `/api/metadata/{ds_id}/tables/{table}/indexes` | 索引信息 | P1 |
| GET | `/api/metadata/{ds_id}/tables/{table}/ddl` | 获取 DDL | P1 |
| GET | `/api/metadata/{ds_id}/tables/{table}/data` | 表数据（?page=&size=） | P1 |
| POST | `/api/metadata/{ds_id}/tables/{table}/alter` | 表结构编辑 | P1 |
| GET | `/api/metadata/{ds_id}/views` | 视图列表 | P1 |
| GET | `/api/metadata/{ds_id}/views/{name}/ddl` | 视图 DDL | P1 |
| GET | `/api/metadata/{ds_id}/functions` | 函数列表 | P1 |
| GET | `/api/metadata/{ds_id}/procedures` | 存储过程列表 | P1 |
| GET | `/api/metadata/{ds_id}/triggers` | 触发器列表 | P2 |
| GET | `/api/metadata/{ds_id}/triggers/{name}/ddl` | 触发器 DDL | P2 |
| GET | `/api/metadata/{ds_id}/sequences` | 序列列表 | P2 |

#### 6.2.4 AI Agent
| 方法 | 端点 | 说明 | 优先级 |
|------|------|------|--------|
| POST | `/api/agent/chat` | Agent 对话（SSE 流式） | P0 |
| POST | `/api/agent/sessions` | 创建会话 | P0 |
| GET | `/api/agent/sessions` | 会话列表 | P0 |
| GET | `/api/agent/sessions/:id/messages` | 历史消息 | P0 |
| DELETE | `/api/agent/sessions/:id` | 删除会话 | P1 |
| POST | `/api/agent/explain` | 解释 SQL | P1 |
| POST | `/api/agent/optimize` | 优化 SQL 建议 | P1 |

#### 6.2.5 文档导出
| 方法 | 端点 | 说明 | 优先级 |
|------|------|------|--------|
| POST | `/api/export/result?format=excel|csv|json` | 导出查询结果 | P1 |
| POST | `/api/export/database?format=word|excel|markdown|html` | 导出数据库文档 | P1 |
| GET | `/api/export/database/status/{task_id}` | 导出任务状态查询 | P2 |

#### 6.2.6 系统管理
| 方法 | 端点 | 说明 | 优先级 |
|------|------|------|--------|
| POST | `/api/auth/login` | 用户登录 | P0 |
| POST | `/api/auth/logout` | 用户登出 | P0 |
| GET | `/api/auth/me` | 当前用户信息 | P0 |
| GET | `/api/config/ai` | AI 配置列表 | P0 |
| POST | `/api/config/ai` | 新增 AI 配置 | P0 |
| PUT | `/api/config/ai/:id` | 更新 AI 配置 | P0 |
| DELETE | `/api/config/ai/:id` | 删除 AI 配置 | P1 |
| PUT | `/api/config/ai/:id/default` | 设为默认模型 | P1 |
| GET | `/api/config/drivers` | JDBC 驱动列表 | P1 |
| POST | `/api/config/drivers` | 上传 JDBC 驱动 | P1 |
| DELETE | `/api/config/drivers/:id` | 删除驱动 | P2 |
| GET | `/api/config/settings` | 系统设置 | P1 |
| PUT | `/api/config/settings` | 更新系统设置 | P1 |

#### 6.2.7 可视化报表
| 方法 | 端点 | 说明 | 优先级 |
|------|------|------|--------|
| POST | `/api/charts` | 创建图表 | P2 |
| GET | `/api/charts` | 图表列表 | P2 |
| PUT | `/api/charts/:id` | 更新图表 | P2 |
| DELETE | `/api/charts/:id` | 删除图表 | P2 |
| POST | `/api/dashboards` | 创建仪表盘 | P2 |
| GET | `/api/dashboards` | 仪表盘列表 | P2 |
| PUT | `/api/dashboards/:id` | 更新仪表盘 | P2 |
| DELETE | `/api/dashboards/:id` | 删除仪表盘 | P2 |

> **修订说明**：补充完整的 API 端点清单，包括克隆连接、触发器/序列/视图/函数元数据、表结构编辑、导出格式区分、图表/仪表盘等此前缺失的端点。

---

## 7. UI/UX 设计

### 7.1 页面路由

| 路由 | 页面 | 说明 |
|------|------|------|
| `/workspace` | SQL 工作台 | 核心页面，默认首页 |
| `/connections` | 连接管理列表 | 数据源管理 |
| `/connections/new` | 新建连接 | 创建数据源 |
| `/connections/:id` | 编辑连接 | 编辑已有数据源 |
| `/agent` | **已移除** | Agent 面板为工作台内浮动组件，非独立页面 |
| `/dashboard` | 可视化报表面板 | P2 阶段实现 |
| `/settings` | 系统设置 | 全局配置 |

> **修订说明**：移除独立的 `/agent` 路由，Agent 面板是工作台内的浮动组件，不与工作台分属不同页面；数据共享（同一个数据源上下文）。

### 7.2 页面线框图

```
┌─────────────────────────────────────────────────────────────────┐
│  [logo] 数据库Agent Web应用    [设置⚙️] [用户👤]                │
├─────────────────────────────────────────────────────────────────┤
│ ┌──────────────┐ ┌──────────────────────────┐ ┌──────────────┐  │
│ │ 📁 数据库对象树 │ │  SQL 编辑器 (多Tab)     │ │  AI Agent   │  │
│ │              │ │  ┌─Tab1─┬─Tab2─┬─Tab3─┐ │ │  面板        │  │
│ │ 📁 server    │ │  │  SELECT * FROM      │ │ │  ┌────────┐  │  │
│ │  ├─📁 schema1 │ │  │  users WHERE       │ │ │  │ 对话    │  │  │
│ │  │  ├─ 表     │ │  │  age > 18          │ │ │  │ 历史    │  │  │
│ │  │  │ ├─ users│ │  │                    │ │ │  │        │  │  │
│ │  │  │ ├─ orders│ │  │  [▶ 执行] [✨ 格式化]│ │ │  │ 用户:  │  │  │
│ │  │  │ └─ ... │ │  └────────────────────┘ │ │  │ 查询... │  │  │
│ │  │  ├─ 视图  │ ├──────────────────────────┤ │  │ ────── │  │  │
│ │  │  ├─ 函数  │ │  结果集表格              │ │  │ Agent: │  │  │
│ │  │  ├─ 序列  │ │  id│name│age│email      │ │  │ SELECT..│  │  │
│ │  │  └─ 触发器│ │  1 │ xx │ 20│ x@x.com   │ │  │ [执行]  │  │  │
│ │  └──────────┘ │  ...                      │ │  └────────┘  │  │
│ │  [收起 ▶]    │  [共 100 条 第1-20条]     │ │  [收起 ▼]    │  │
│ └──────────────┘ └──────────────────────────┘ └──────────────┘  │
│                ↕ 可拖拽分割线 ↕                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 7.3 关键交互细节

**Agent 面板与工作台的关系**：
- Agent 面板是 **SQL 工作台页面内的浮动组件**，不是独立页面
- 面板默认固定在右下角，可拖拽移动、可收起、可调整大小
- 面板共享工作台当前选中的**数据源上下文**
- 当未选择数据源时，面板显示引导提示："请先连接一个数据源"
- 切换工作台数据源时，Agent 面板弹出提示："数据源已切换，是否保留当前对话？"

**可点击 SQL 片段的交互**：
- Agent 回复中的 SQL 代码块右下角显示 **「填入编辑器」** 和 **「执行」** 两个按钮
- **「填入编辑器」**：SQL 填入当前活动 Tab 的光标位置（不覆盖现有内容），用户可编辑后再执行
- **「执行」**：SQL 覆盖当前 Tab 内容并立即执行，执行前弹出确认框（写操作时）
- 两种操作都不自动新建 Tab（避免 Tab 泛滥）

> **修订说明**：明确 Agent 面板与工作台的从属关系和数据共享方式；补齐可点击 SQL 的完整交互定义。

---

## 8. 验收标准

### 8.1 功能验收

- [ ] 支持连接 **Oracle、OceanBase、GoldenDB** 三种数据库（P0），连接测试功能正常
- [ ] P1 阶段支持 MySQL、PostgreSQL、SQLite 连接
- [ ] SQL 编辑器语法高亮正常，可执行 SQL 并正确返回结果
- [ ] 多 Tab 独立工作，切换 Tab 不丢失编辑内容和查询结果
- [ ] AI Agent 可根据自然语言生成正确 SQL
- [ ] AI Agent **正确率标准**：针对预定义的 20 条测试查询用例，SQL 生成语法正确率 ≥ 90%，语义正确率 ≥ 80%
- [ ] Agent 对话保持上下文（5 轮以上连续对话无上下文丢失）
- [ ] 支持导出数据库文档（至少 Word 格式）
- [ ] 连接信息加密存储，密码不泄露
- [ ] SQL 执行安全机制正常工作（只读自动放行、DML 确认、DDL 警告）

### 8.2 技术验收

- [ ] 新增数据库类型只需实现适配器接口（无需改核心代码）
- [ ] **首次加载时间 ≤ 3s**（非 5MB 硬限制）；CodeMirror 和 ECharts 允许 CDN 外链加载
- [ ] API 响应格式统一（code + message + data）
- [ ] 单元测试覆盖率 > 60%
- [ ] Docker 一键启动
- [ ] SSE 流式输出断线自动重连（3 次尝试）

> **修订说明**：AI Agent 验收增加量化标准（20 条测试用例，语法正确率 ≥ 90%，语义正确率 ≥ 80%）；前端构建从"< 5MB"改为"首次加载 ≤ 3s"并允许 CDN。

---

## 9. 项目交付物

### 9.1 阶段规划

| 阶段 | 周期 | 数据库支持 | 交付内容 |
|------|------|------------|----------|
| **P0 - MVP** | 6-8 周 | Oracle、OceanBase、GoldenDB | 连接管理 + SQL 工作台（编辑器+执行+结果+多Tab）+ AI Agent 自然语言→SQL（OpenAI）+ 基础系统管理（认证+AI 配置） |
| **P1 - 核心增强** | 4-6 周 | 增加 MySQL、PostgreSQL、SQLite | SSH 隧道 + 多环境 + 表结构可视化编辑 + AI 多模型（Claude/Ollama）+ 文档导出（Word/Excel）+ 执行历史 + 克隆连接 + 视图/函数/存储过程管理 |
| **P2 - 扩展** | 4-6 周 | 增加 MongoDB | 可视化报表（图表+仪表盘）+ 异常诊断 + 执行计划分析 + 触发器/序列管理 + JDBC 驱动管理 + Markdown/HTML 导出 |
| **P3 - 完善** | 持续 | — | 团队协作 + 仪表盘分享 + 插件市场 + i18n 完善 + 审计日志面板 + 自定义 Prompt |

### 9.2 文档交付物
- 需求说明书（本文档 V2.0）
- 系统设计说明书
- API 文档（OpenAPI / Swagger）
- 数据库设计文档
- 部署运维手册
- 用户使用手册
- AI Agent 测试查询用例集（20 条，覆盖简单查询、多表 JOIN、聚合、子查询等场景）

> **修订说明**：阶段规划中明确每阶段支持的数据库类型，与 2.2.1 保持一致；增加 AI Agent 测试用例集的交付要求。

---

## 10. 附录

### 10.1 术语表

| 术语 | 含义 |
|------|------|
| Agent | 具备自主推理和工具调用能力的 AI 智能体 |
| DDL | 数据定义语言（CREATE/ALTER/DROP） |
| DML | 数据操作语言（SELECT/INSERT/UPDATE/DELETE） |
| Schema | 数据库模式（Oracle/PostgreSQL 中的命名空间） |
| SSE | Server-Sent Events，服务端推送技术 |
| SPI | 服务提供者接口，插件扩展机制 |
| ReAct | Reasoning + Acting，Agent 推理模式 |
| Fernet | cryptography 库提供的对称加密方案 |

### 10.2 参考项目
- [Chat2DB](https://github.com/OtterMind/Chat2DB) — 开源数据库客户端（参考架构设计）
- [LangChain](https://github.com/langchain-ai/langchain) — LLM 应用框架（Agent 参考）
- [FastAPI](https://github.com/fastapi/fastapi) — Python Web 框架
- [Naive UI](https://github.com/tusen-ai/naive-ui) — Vue 3 组件库

---

### 附：V1.0 → V2.0 修订对照表

| 编号 | 问题分类 | 问题描述 | V2.0 修订措施 |
|------|----------|----------|---------------|
| 1 | 编号错误 | 全景图 3.x.x 应属第 2 章 | 修正为 2.x.x |
| 2 | 编号错误 | 2.2.4/2.2.5 等编号错位 | 重新编号为 2.5/2.6/2.7/2.8/2.9 |
| 3 | 优先级矛盾 | 多 Tab 在 API 表为 P1，UI 原型展示为核心 | 提升为 P0 |
| 4 | 功能边界 | 数据库支持范围各章节不一致 | 分阶段明确定义：P0=Oracle/OceanBase/GoldenDB |
| 5 | 功能边界 | "桌面端免登录"与 Web 应用定位矛盾 | 移除，项目定位为纯 Web |
| 6 | 功能边界 | SQL 执行授权机制未定义 | 补充完整授权矩阵 + 前后端交互协议（execution_id、risk_level、/execute/confirm 端点） |
| 7 | 数据模型 | Table 实体未说明持久化策略 | 明确：动态查询，不持久化；收藏状态持久化 |
| 8 | 数据模型 | Chart/Dashboard 实体缺失 | 补充完整实体定义 |
| 9 | 数据模型 | User 实体缺失 | 补充完整实体定义 |
| 10 | 数据模型 | AIConfig 实体缺失 | 补充完整实体定义 |
| 11 | 数据模型 | JdbcDriver 实体缺失 | 补充完整实体定义 |
| 12 | API 不全 | 克隆连接无端点 | 补充 `POST /api/connections/:id/clone` |
| 13 | API 不全 | 触发器/序列无元数据 API | 补充 triggers/sequences/functions/procedures 端点 |
| 14 | API 不全 | 表结构编辑无 API | 补充 `POST /api/metadata/:id/tables/:table/alter` |
| 15 | API 不全 | 导出格式区分方式未定义 | 统一通过 `?format=` 参数区分 |
| 16 | 非功能模糊 | SQL 注入防护参数化查询不适用 | 改为授权+审计方案，承认场景特殊性 |
| 17 | 非功能模糊 | "断点续传"含义不明 | 明确：指导出任务中断可恢复，非下载断点续传 |
| 18 | 非功能模糊 | 多模型切换策略未定义 | 补充完整切换策略（系统级/对话级、不可中途切换） |
| 19 | UI 模糊 | Agent 面板与工作台关系不明 | 明确为工作台内浮动组件，非独立页面 |
| 20 | UI 模糊 | 可点击 SQL 交互未定义 | 补充"填入编辑器"和"执行"两种按钮行为 |
| 21 | 验收模糊 | AI Agent 正确率无量化标准 | 补充 20 条测试用例，语法 ≥ 90%，语义 ≥ 80% |
| 22 | 验收模糊 | 前端 5MB 硬门槛不合理 | 改为"首次加载 ≤ 3s"，允许 CDN + 按需加载 |
| 23 | 协议缺失 | 危险操作二次确认前后端协议未定义 | 补充完整协议（请求-响应 Schema、execution_id、risk_level、/execute/confirm 端点） |
| 24 | 协议缺失 | Agent SQL 结果回传方式未定义 | 补充 SSE 事件类型定义、result 格式、大结果集截断策略、前端渲染要求 |

---



不同角色看到的页面内容和权限不同，管理员可以看到所有页面，并可以通过用户管理页面配置用户角色信息，可以配置不同角色的人能看到哪些页面。



*文档版本：v3.0*
*最后更新：2026-08-12*

